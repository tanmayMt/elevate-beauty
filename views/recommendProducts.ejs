<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recommended Products</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        /* General styling */
        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.6;
            margin: 0;
            background-color: #f9f9f9;
            color: #333;
        }
    
        /* Header styling */
        header {
            background-color: #ff6f61;
            color: white;
            text-align: center;
            padding: 2rem;
        }
    
        header h1 {
            font-size: 2.5rem;
            margin: 0;
        }
    
        header p {
            font-size: 1.2rem;
        }
    
        /* Main container */
        .container {
            padding: 2rem;
        }
    
        /* Product cards */
        .products {
            display: flex;
            flex-wrap: wrap;
            gap: 2rem;
            justify-content: center;
        }
    
        .card {
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            text-align: center;
            width: 300px;
            padding: 1rem;
            transition: transform 0.3s, box-shadow 0.3s;
        }
    
        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 12px rgba(0, 0, 0, 0.2);
        }
    
        .product-image {
            width: 100%;
            border-radius: 10px;
            margin-bottom: 1rem;
        }
    
        h2 {
            font-size: 1.5rem;
            color: #333;
        }
    
        p {
            margin: 0.5rem 0;
        }
    
        strong {
            color: #ff6f61;
        }
    
        /* Buttons */
        .btn {
            background-color: #ff6f61;
            color: white;
            border: none;
            padding: 0.8rem 1.2rem;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1rem;
            margin-top: 1rem;
            transition: background-color 0.3s;
        }
    
        .btn:hover {
            background-color: #e05b50;
        }
    
        .buy-now {
            background-color: #4caf50;
        }
    
        .buy-now:hover {
            background-color: #3e9140;
        }
    
        /* Footer */
        footer {
            text-align: center;
            background-color: #333;
            color: white;
            padding: 1rem 0;
            margin-top: 2rem;
        }
    </style>
</head>

<body>
    <header>
        <h1>Recommended Products</h1>
        <p>Based on your quiz results, we’ve curated these just for you!</p>
    </header>

    <main class="container">
        <div class="products">
            <% if (products && products.length> 0) { %>
                <% products.forEach(product=> { %>
                    <div class="card product">
                        <img src="/images/<%= product.image %>" alt="<%= product.name %>" class="product-image">
                        <h2>
                            <%= product.name %>
                        </h2>
                        <p>
                            <%= product.description %>
                        </p>
                        <p><strong>Price:</strong> $<%= product.price %>
                        </p>
                        <form action="/cart" method="POST" class="add-to-cart-form">
                            <input type="hidden" name="productId" value="<%= product._id %>">
                            <button type="submit" class="btn">Add to Cart</button>
                        </form>
                        <form action="/payment/checkout" method="GET">
                            <input type="hidden" name="productId" value="<%= product._id %>">
                            <button type="submit" class="btn buy-now">Buy Now</button>
                        </form>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <p>No recommended products found. Please retake the quiz to see suggestions.</p>
                            <% } %>
        </div>
    </main>

    <footer>
        <p>&copy; 2024 Elevate Your Beauty. All rights reserved.</p>
    </footer>
</body>

</html>